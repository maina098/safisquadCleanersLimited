import { useEffect, useState } from 'react'
import './App.css'

const fallbackContent = {
  cities: '5+', stores: 5, garments: '5,000',
  services: ['Dry cleaning', 'Laundry', 'Steam press', 'Shoe care', 'Home cleaning', 'House cleaning', 'Compound cleaning', 'Toilet cleaning', "Airbnb's cleaning", 'Office cleanup'],
  sigma: ['Unique barcode tags', 'Specialised segregation', 'Wash care instructions', 'Fabric-friendly detergents', 'Steam press', 'Three-stage quality check'],
}
const garmentTypes = [
  ['Clothes', 'shirt.jpg'], ['Airbnbs', 'trousers.jpg'], ['Toilet', 'lehenga.jpg'], ['Housing compound', 'saree.jpg'],
]
const serviceDescriptions = {
  'Dry cleaning': 'Gentle off-site care for everyday and occasion wear.',
  Laundry: 'Wash, fold and finish, just the way you like it.',
  'Steam press': 'Crisp, crease-free garments ready when you are.',
  'Shoe care': 'Expert care for shoes, bags and accessories.',
  'Home cleaning': 'A complete clean for the spaces you live in.',
  'House cleaning': 'Reliable cleaning for every room in your home.',
  'Compound cleaning': 'Fresh, tidy outdoor spaces from edge to edge.',
  'Toilet cleaning': 'Detailed sanitation for a hygienic, fresh bathroom.',
  "Airbnb's cleaning": 'Fast, thorough turnovers ready for your next guest.',
  'Office cleanup': 'A polished, productive workspace for your team.',
}
const apiBase = import.meta.env.VITE_API_URL || 'http://localhost:4000'
const pricing = [
  { service: 'House deep-cleaning', unit: 'per visit', price: 3500, description: 'Kitchen, bathrooms, living spaces and bedrooms.' },
  { service: 'Laundry', unit: 'per kilogram', price: 250, description: 'Wash, dry and fold with careful sorting.' },
  { service: 'Carpet cleaning', unit: 'per square metre', price: 180, description: 'Deep extraction and stain treatment.' },
  { service: 'Office cleaning contract', unit: 'per month', price: 15000, description: 'Reliable recurring care for productive offices.' },
]
const formatMoney = (amount) => `KES ${Number(amount).toLocaleString()}`

function App() {
  const [content, setContent] = useState(fallbackContent)
  const [menuOpen, setMenuOpen] = useState(false)
  const [bookingOpen, setBookingOpen] = useState(false)
  const [portalOpen, setPortalOpen] = useState(false)
  const [portalLoggedIn, setPortalLoggedIn] = useState(() => localStorage.getItem('safisquad-portal-session') === 'active' && Boolean(localStorage.getItem('safisquad-portal-token')))
  const [portalError, setPortalError] = useState('')
  const [submitted, setSubmitted] = useState(false)
  const [selectedService, setSelectedService] = useState('Dry cleaning')
  const [trackingCode, setTrackingCode] = useState('')
  const [trackedOrder, setTrackedOrder] = useState(null)
  const [orders, setOrders] = useState([])
  const [calculator, setCalculator] = useState({ service: 'House deep-cleaning', quantity: 1 })
  useEffect(() => {
    fetch(`${apiBase}/api/site-content`).then((response) => response.ok ? response.json() : Promise.reject()).then((data) => setContent({ ...fallbackContent, ...data })).catch(() => {})
  }, [])
  const openBooking = (service = 'Dry cleaning') => { setSelectedService(service); setSubmitted(false); setBookingOpen(true) }
  const submitBooking = async (event) => { event.preventDefault(); const formData = new FormData(event.currentTarget); const response = await fetch(`${apiBase}/api/orders`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(Object.fromEntries(formData)) }); if (response.ok) { const data = await response.json(); setTrackedOrder(data.order); setTrackingCode(data.order.tracking_code); setSubmitted(true) } }
  const lookupOrder = async (event) => { event.preventDefault(); const response = await fetch(`${apiBase}/api/orders/${trackingCode.trim()}`); setTrackedOrder(response.ok ? (await response.json()).order : null) }
  const portalHeaders = () => { const token = localStorage.getItem('safisquad-portal-token'); return token ? { Authorization: `Bearer ${token}` } : {} }
  const loadOrders = async () => { const response = await fetch(`${apiBase}/api/orders`, { headers: portalHeaders() }); if (response.ok) setOrders((await response.json()).orders) }
  const updateStatus = async (id, status) => { const response = await fetch(`${apiBase}/api/orders/${id}/status`, { method: 'PATCH', headers: { 'Content-Type': 'application/json', ...portalHeaders() }, body: JSON.stringify({ status }) }); if (response.ok) loadOrders() }
  const loginToPortal = async (event) => { event.preventDefault(); const formData = new FormData(event.currentTarget); if (!formData.get('email') || !formData.get('password')) return; const response = await fetch(`${apiBase}/api/auth/login`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(Object.fromEntries(formData)) }); if (!response.ok) { setPortalError((await response.json()).error || 'Login failed.'); return; } const data = await response.json(); localStorage.setItem('safisquad-portal-session', 'active'); localStorage.setItem('safisquad-portal-token', data.token); setPortalError(''); window.history.replaceState(null, '', '#admin'); setPortalLoggedIn(true); setPortalOpen(true); loadOrders() }
  const logoutPortal = () => { localStorage.removeItem('safisquad-portal-session'); localStorage.removeItem('safisquad-portal-token'); setPortalLoggedIn(false); setPortalOpen(false) }
  const selectedPrice = pricing.find((item) => item.service === calculator.service) || pricing[0]
  const estimate = selectedPrice.price * Math.max(1, Number(calculator.quantity) || 1)

  return <div className="site-shell">
    <header className="site-header"><a className="logo" href="#top"><span>C</span><strong>cleaning<span>squad</span></strong></a><button className="menu-toggle" onClick={() => setMenuOpen(!menuOpen)} aria-label="Toggle navigation">☰</button><nav className={menuOpen ? 'site-nav open' : 'site-nav'}><a href="#services">Services</a><a href="#pricing">Pricing</a><a href="#tracking">Track order</a><a href="#how-it-works">How it works</a><button className="portal-link" onClick={() => { setPortalOpen(true); if (portalLoggedIn) loadOrders() }}>{portalLoggedIn ? 'Admin panel' : 'Portal'}</button><button className="nav-cta" onClick={() => openBooking()}>Book a pickup <span>↗</span></button></nav></header>
    <main id="top">
      <section className="hero"><div className="hero-copy"><p className="kicker">MOMBASA'S HYBRID CLEANING & CARE SERVICE</p><h1>Fresh clothes.<br /><em>Fresh feeling.</em></h1><p className="hero-text">Professional garment care off-site, plus dependable cleaning for homes, Airbnbs and offices on-site.</p><button className="button dark" onClick={() => openBooking()}>Schedule a pickup <span>↗</span></button><div className="hero-note"><span>✦</span> Free doorstep pickup and delivery</div></div><div className="hero-visual"><img src="/fabricspa.com/wp-content/uploads/2026/01/website-banners_Artboard-1-copy-3-scaled.jpg" alt="Dress and garments ready for professional care" /><div className="floating-card"><strong>5+ years</strong><span>Safi Squad care</span></div></div></section>
      <section className="trust-strip"><p>SAFISQUAD'S HYBRID CLEANING AND GARMENT CARE SERVICE</p><div><strong>5+</strong><span>years of care</span></div><div><strong>{content.cities}</strong><span>subcounties served</span></div><div><strong>{content.stores}+</strong><span>stores across Mombasa</span></div><div><strong>{content.garments}</strong><span>garments weekly</span></div></section>
      <section className="intro section"><div className="section-label">01 / WHAT WE DO</div><div className="intro-grid"><h2>Your precious garments,<br /><em>our devoted care.</em></h2><div><p>We handle couture, heirlooms and your most treasured garments. Whether it is a cherished saree, a wedding lehenga, or your favourite designer outfit, we treat every garment with the care it deserves.</p><a className="arrow-link" href="/fabricspa.com/about-us/">Our story <span>↗</span></a></div></div></section>
      <section className="services section" id="services"><div className="section-label">02 / OUR SERVICES</div><div className="section-heading"><h2>Care<br /><em>your way.</em></h2><p>Garment care off-site and cleaning services on-site for homes, Airbnbs, compounds, toilets and offices.</p></div><div className="service-grid">{content.services.map((service, index) => <article className="service-card" key={service}><span className="service-number">{String(index + 1).padStart(2, '0')}</span><div className={`service-art art-${(index % 4) + 1}`}><span>{['◌', '⌁', '≋', '◇'][index % 4]}</span></div><h3>{service}</h3><p>{serviceDescriptions[service] || 'Thoughtful cleaning and care delivered with signature attention.'}</p><button onClick={() => openBooking(service)}>Book this service <span>↗</span></button></article>)}</div></section>
      <section className="pricing-section section" id="pricing"><div className="section-label">02A / CLEAR PRICING</div><div className="section-heading"><h2>Know the cost<br /><em>before you book.</em></h2><p>Simple starting rates in Kenyan shillings. Your final quote is confirmed after we review the service details.</p></div><div className="price-grid">{pricing.map((item) => <article className="price-card" key={item.service}><span>{item.unit}</span><h3>{item.service}</h3><strong>{formatMoney(item.price)}</strong><p>{item.description}</p><button onClick={() => openBooking(item.service)}>Book service <span>↗</span></button></article>)}</div><div className="calculator"><div><div className="section-label">QUICK ESTIMATE</div><h3>Build your estimate</h3><p>Adjust the quantity to see a live guide price.</p></div><label>Service<select value={calculator.service} onChange={(event) => setCalculator({ ...calculator, service: event.target.value })}>{pricing.map((item) => <option key={item.service}>{item.service}</option>)}</select></label><label>Quantity<input type="number" min="1" value={calculator.quantity} onChange={(event) => setCalculator({ ...calculator, quantity: event.target.value })} /></label><div className="estimate"><span>Estimated total</span><strong>{formatMoney(estimate)}</strong><small>{selectedPrice.unit}</small></div></div></section>
      <section className="tracking-section section" id="tracking"><div className="section-label">02B / ORDER TRACKING</div><div className="tracking-grid"><div><h2>See every step<br /><em>of the journey.</em></h2><p>Enter the tracking code from your booking confirmation to see the live care status.</p><form className="tracking-form" onSubmit={lookupOrder}><input required value={trackingCode} onChange={(event) => setTrackingCode(event.target.value)} placeholder="SQ-XXXXXXXX" /><button className="button dark">Track order <span>↗</span></button></form>{trackedOrder && <div className="order-result"><strong>{trackedOrder.tracking_code}</strong><span>{trackedOrder.service} · {formatMoney(trackedOrder.estimated_cost)}</span><OrderTimeline status={trackedOrder.status} events={trackedOrder.events} /></div>}</div><div className="trust-panel"><span>YOUR DETAILS STAY VISIBLE</span><h3>One record from booking to delivery.</h3><p>Your address, requested service, estimate and status history are stored together for the care team and customer.</p></div></div></section>
      <section className="admin-section section" id="operations"><div className="section-label">OPERATIONS / LIVE ORDERS</div><div className="section-heading"><h2>Care team<br /><em>control room.</em></h2><button className="button outline" onClick={loadOrders}>Refresh orders ↻</button></div><div className="admin-list">{orders.length ? orders.map((order) => <article className="admin-order" key={order.id}><div><strong>{order.tracking_code}</strong><span>{order.customer_name} · {order.service}</span><small>{order.address} · {order.preferred_date}</small></div><select value={order.status} onChange={(event) => updateStatus(order.id, event.target.value)}>{['Pending', 'Picked Up', 'In-Progress', 'QC Passed', 'Out for Delivery', 'Delivered'].map((status) => <option key={status}>{status}</option>)}</select></article>) : <p className="empty-state">Refresh to load recorded bookings.</p>}</div></section>
      <section className="split-section" id="how-it-works"><div className="split-image"><img src="/fabricspa.com/wp-content/uploads/2025/07/work-img-1.jpg" alt="Safi Squad pickup and delivery" /></div><div className="split-copy"><div className="section-label">03 / HOW IT WORKS</div><h2>Clean garments,<br /><em>zero effort.</em></h2><div className="steps"><Step number="01" title="Schedule online" text="Choose pickup and drop-off times that work best for you." /><Step number="02" title="We pick up" text="A Safi Squad agent collects your garments from your doorstep." /><Step number="03" title="We deliver" text="Your garments return clean, fresh and wrinkle-free." /></div><button className="button outline" onClick={() => openBooking()}>Start with a pickup <span>↗</span></button></div></section>
      <section className="care-section section"><div className="section-label">04 / CARE THAT FITS YOU</div><h2>Because every garment<br /><em>has its own story.</em></h2><div className="care-grid"><Care title="Regular" text="Thoughtful care, right on time. Everyday garments handled with signature attention." image="care-img-1.svg" /><Care title="Express" text="When you need it fresh, fast and flawless, for life's last-minute plans." image="care-img-2.svg" /><Care title="Premium" text="For garments that cannot afford shortcuts. Reserved for your most prized pieces." image="care-img-3.svg" /></div></section>
      <section className="sigma section" id="sigmacare"><div className="section-label">05 / THE CLEANING SQUAD DIFFERENCE</div><div className="sigma-head"><h2>Experience the nuances of<br /><em>9-step care</em></h2><p>A considered process built around your clothes, so they look better and feel better for longer.</p></div><div className="sigma-list">{content.sigma.map((step, index) => <div key={step}><strong>0{index + 1}</strong><span>{step}</span></div>)}</div></section>
      <section className="garments section" id="garments"><div className="section-label">06 / WE CARE BY THE PIECE</div><div className="section-heading"><h2>From head to toe,<br /><em>we have you covered.</em></h2><p>Expert treatment for clothes and accessories across every part of your wardrobe.</p></div><div className="garment-grid">{garmentTypes.map(([name, image]) => <button className="garment-card" key={name} onClick={() => openBooking(name)}><img src={`/fabricspa.com/wp-content/uploads/2025/08/${image}`} alt={name} /><span>{name}</span><small>Explore care ↗</small></button>)}</div></section>
      <section className="quote-section"><p>“We do not deal in loads.”</p><h2>We care<br /><em>by the piece.</em></h2><span>Thousands of customers trust us with their most-loved clothes.</span></section>
      <section className="app-banner"><div><p className="kicker">YOUR CLOSET, IN YOUR POCKET</p><h2>More care.<br /><em>One tap away.</em></h2><p>Book pickups, track orders and find your nearest Cleaning Squad store on the app.</p><button className="button light" onClick={() => openBooking()}>Get started <span>↗</span></button></div><img src="/fabricspa.com/wp-content/uploads/2025/09/app-mob-img-1.png" alt="Cleaning Squad mobile app" /></section>
    </main>
    <footer className="site-footer"><a className="logo" href="#top"><span>C</span><strong>cleaning<span>squad</span></strong></a><p>Because clothes carry more than just a story.</p><div className="footer-links"><a href="/fabricspa.com/about-us/">About us</a><a href="/fabricspa.com/privacy-policy/">Privacy</a><a href="/fabricspa.com/terms-condition/">Terms</a><a href="/fabricspa.com/contact-us/">Contact</a></div><div className="contact-details"><a href="tel:+254708309425">+254 708 309425</a><a href="mailto:safisquaadcleaningservices@gmail.com">safisquaadcleaningservices@gmail.com</a></div><small>© {new Date().getFullYear()} Safi Squad Cleaning Services Limited.</small></footer>
    {portalOpen && <div className="portal-backdrop" onClick={() => setPortalOpen(false)}><div className="portal-modal" onClick={(event) => event.stopPropagation()}><button className="modal-close" onClick={() => setPortalOpen(false)} aria-label="Close portal">×</button>{portalLoggedIn ? <div className="portal-dashboard"><aside className="portal-sidebar"><strong className="portal-brand"><span>S</span> SafiCleaners</strong><p>OPERATIONS PORTAL</p><button className="portal-nav-active">▦ Dashboard</button><button onClick={loadOrders}>▤ Orders</button><button>◷ Workflow</button><button>♙ Team</button><button>▥ Settlements</button><button className="portal-logout" onClick={logoutPortal}>Log out</button></aside><main className="portal-main"><div className="portal-topbar"><div><p className="kicker">OPERATIONS OVERVIEW</p><h2>Welcome back,<br /><em>care team.</em></h2></div><span className="portal-role">ADMIN CONSOLE</span></div><div className="portal-stats"><div><span>Active orders</span><strong>{orders.filter((order) => order.status !== 'Delivered').length}</strong></div><div><span>Pending pickup</span><strong>{orders.filter((order) => order.status === 'Pending').length}</strong></div><div><span>QC queue</span><strong>{orders.filter((order) => order.status === 'QC Passed').length}</strong></div></div><section className="portal-order-section"><div className="portal-section-heading"><h3>Recent orders</h3><button onClick={loadOrders}>Refresh ↻</button></div><div className="portal-orders">{orders.length ? orders.map((order) => <article className="admin-order" key={order.id}><div><strong>{order.tracking_code}</strong><span>{order.customer_name} · {order.service}</span><small>{order.address} · {order.preferred_date} · Deposit {formatMoney(order.deposit_paid || 0)}</small></div><select value={order.status} onChange={(event) => updateStatus(order.id, event.target.value)}>{['Pending', 'Picked Up', 'In-Progress', 'QC Passed', 'Out for Delivery', 'Delivered'].map((status) => <option key={status}>{status}</option>)}</select></article>) : <p className="empty-state">No bookings recorded yet.</p>}</div></section><div className="portal-review-card"><span>SETTLEMENTS</span><strong>Weekly payout review gate</strong><p>Calculate and approve payouts before any B2C disbursement is triggered.</p><button>Open settlement review →</button></div><p className="portal-contact">Any queries? Email <a href="mailto:safisquaadcleaningservices@gmail.com">safisquaadcleaningservices@gmail.com</a>.</p></main></div> : <><p className="kicker">TEAM ACCESS</p><h2>Welcome to the<br /><em>SafiCleaners portal.</em></h2><p className="portal-intro">Sign in to view bookings and manage the order journey from pickup to delivery.</p><form className="portal-form" onSubmit={loginToPortal}><label>Email<input required name="email" type="email" placeholder="you@saficleaners.com" /></label><label>Password<input required name="password" type="password" placeholder="Enter your password" /></label>{portalError && <p className="portal-error">{portalError}</p>}<button className="button dark" type="submit">Log in to admin panel <span>↗</span></button></form><p className="portal-contact">Any queries? Email <a href="mailto:safisquaadcleaningservices@gmail.com">safisquaadcleaningservices@gmail.com</a>.</p></>}</div></div>}
    {bookingOpen && <div className="modal-backdrop" onClick={() => setBookingOpen(false)}><div className="booking-modal" onClick={(event) => event.stopPropagation()}><button className="modal-close" onClick={() => setBookingOpen(false)} aria-label="Close">×</button>{submitted ? <div className="success"><span>✓</span><h2>Booking recorded.</h2><p>Your tracking code is <strong>{trackingCode}</strong>. Keep it to follow every stage of your order.</p><button className="button dark" onClick={() => setBookingOpen(false)}>Done</button></div> : <><p className="kicker">DOORSTEP BOOKING</p><h2>Let us take care<br /><em>of the rest.</em></h2><form onSubmit={submitBooking}><label>Name<input required name="name" placeholder="Your name" /></label><label>Phone<input required name="phone" type="tel" placeholder="10-digit mobile number" /></label><label>Email<input name="email" type="email" placeholder="you@example.com" /></label><label>Service<select name="service" value={selectedService} onChange={(event) => setSelectedService(event.target.value)}>{[...content.services, ...pricing.map((item) => item.service)].filter((service, index, list) => list.indexOf(service) === index).map((service) => <option key={service}>{service}</option>)}</select></label><label>Preferred date<input required name="preferredDate" type="date" /></label><label>Address<input required name="address" placeholder="Full pickup or service address" /></label><label>Service specifics<textarea name="details" placeholder="Items, rooms, carpet size or contract needs" /></label><input type="hidden" name="quantity" value="1" /><input type="hidden" name="estimatedCost" value={pricing.find((item) => item.service === selectedService)?.price || 0} /><button className="button dark" type="submit">Confirm booking <span>↗</span></button></form></>}</div></div>}
  </div>
}

function Step({ number, title, text }) { return <div className="step"><strong>{number}</strong><div><h3>{title}</h3><p>{text}</p></div></div> }
function Care({ title, text, image }) { return <article className="care-card"><img src={`/fabricspa.com/wp-content/uploads/2025/07/${image}`} alt="" /><h3>{title}</h3><p>{text}</p><a href="/fabricspa.com/contact-us/">Choose {title.toLowerCase()} <span>↗</span></a></article> }
function OrderTimeline({ status, events = [] }) { const statuses = ['Pending', 'Picked Up', 'In-Progress', 'QC Passed', 'Out for Delivery', 'Delivered']; const activeIndex = statuses.indexOf(status); return <div className="order-timeline">{statuses.map((item, index) => <div className={index <= activeIndex ? 'timeline-step active' : 'timeline-step'} key={item}><i>{index <= activeIndex ? '✓' : index + 1}</i><span>{item}</span>{events.find((event) => event.status === item) && <small>{new Date(events.find((event) => event.status === item).created_at).toLocaleDateString()}</small>}</div>)}</div> }
export default App
